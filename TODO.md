* github website? (in docs/)
* cpanfile
* semantics for relative/absolute symlinks - auto, --relative, --absolute.  In auto mode, you use abs if the cmdline is absolute, and relative otherwise
* test suite (TAP)
* list active local::lib
